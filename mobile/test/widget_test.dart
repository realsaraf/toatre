import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Skipped: full app requires Firebase init.
    expect(true, isTrue);
  });
}
